SYMBOLS {
    __STACK_START__: type = weak, value = $0700; # 1 pages stack
    __STACK_SIZE__: type = weak, value = $0100; # 1 pages stack
}
MEMORY {
    ZP:     file = "", start = $0002, size = $001A, type = rw, define = yes;

    # INES Cartridge Header
    HEADER: file = %O, start = $0000, size = $0010, fill = yes;

    # 2 16K ROM Banks
    # - startup
    # - code
    # - rodata
    # - data (load)
    ROM0:   file = %O, start = $8000, size = $7FFA, fill = yes, define = yes;

    # Hardware Vectors at End of 2nd 8K ROM
    ROMV:   file = %O, start = $FFFA, size = $0006, fill = yes;

    # 1 8k CHR Bank
    ROM2:   file = %O, start = $0000, size = $2000, fill = yes;

    # standard 2k RAM (-zeropage)
    # $0100-$01FF cpu stack
    # $0200-$04FF 3 pages for ppu memory write buffer
    # $0500-$06FF 2 pages for cc65 data area
    # $0700-$07FF 1 pages for cc65 parameter stack
    RAM:    file = "", start = $0500, size = $0300, define = yes;
}
SEGMENTS {
    HEADER:   load = HEADER,          type = ro;
    STARTUP:  load = ROM0,            type = ro,  define = yes;
    INIT:     load = ROM0,            type = ro,  define = yes, optional = yes;
    CODE:     load = ROM0,            type = ro,  define = yes;
    ONCE:     load = ROM0,            type = ro,  optional = yes;
    RODATA:   load = ROM0,            type = ro,  define = yes;
    DMC:      load = ROM0,            type = ro,  start = $C000, optional = yes;
    DATA:     load = ROM0, run = RAM, type = rw,  define = yes;
    VECTORS:  load = ROMV,            type = ro;
    CHARS:    load = ROM2,            type = ro;
    BSS:      load = RAM,             type = bss, define = yes;
    ZEROPAGE: load = ZP,              type = zp;
}
FEATURES {
    CONDES: type    = constructor,
            label   = __CONSTRUCTOR_TABLE__,
            count   = __CONSTRUCTOR_COUNT__,
            segment = INIT;
    CONDES: type    = destructor,
            label   = __DESTRUCTOR_TABLE__,
            count   = __DESTRUCTOR_COUNT__,
            segment = RODATA;
    CONDES: type    = interruptor,
            label   = __INTERRUPTOR_TABLE__,
            count   = __INTERRUPTOR_COUNT__,
            segment = RODATA,
            import  = __CALLIRQ__;
}
